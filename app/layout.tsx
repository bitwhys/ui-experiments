import type { Metadata } from 'next'
import { Geist, Geist_Mono, Inter_Tight, Syncopate } from 'next/font/google'

import { cx } from '@/lib/utils'

import './globals.css'

import App from 'next/app'

import { AppProvider } from '@/components/app-provider'

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
})

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

const fontSans = Inter_Tight({
  weight: 'variable',
  variable: '--inter-tight',
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

const customFont = Syncopate({
  weight: ['400'],
  variable: '--font-syncopate',
})

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body
        className={cx(
          geistSans.variable,
          geistMono.variable,
          fontSans.variable,
          customFont.variable,
          'bg-page text-cnt-primary font-sans antialiased',
        )}
      >
        <AppProvider defaultThemeGrayColor="sand" defaultThemeAccentColor="tomato">
          {children}
        </AppProvider>
      </body>
    </html>
  )
}
